import React from 'react';

const Register = () => {
    const onSubmit = (data) => { 
        alert('login success')
        
      };
    return (
        <div>
            
        </div>
    );
};

export default Register;