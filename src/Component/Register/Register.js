import React from 'react';

const Register = () => {
    const onSubmit = (data) => { 
        alert('login success')
        
      };
    return (
        <div>
            <h1>This is register</h1>
        </div>
    );
};

export default Register;